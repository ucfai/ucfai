version: "3"
services:
  site:
    command: jekyll s --incremental
    image: jekyll/jekyll:3.8
    volumes:
      - $PWD:/srv/jekyll
      - $PWD/vendor/bundle:/usr/local/bundle
    ports:
      - 8234:4000
      - 8235:35729
      - 8236:3000
